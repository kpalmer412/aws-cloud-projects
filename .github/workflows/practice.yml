name: Practice GitHub Actions

on:
  push:
    branches: [ main ]
  workflow_dispatch: 

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello Ken! Your VS Code Action extension is working."
